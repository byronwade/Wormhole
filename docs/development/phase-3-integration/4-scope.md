In: chunked data plane, RAM cache, prefetch heuristics, FUSE stitching, RO reads.
Out: persistent/on-disk cache, adaptive chunk size, write support, trust UX, connection pooling optimization (can be added later).
