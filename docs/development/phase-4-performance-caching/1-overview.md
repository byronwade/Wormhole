# Phase 4 — Execution Manual
Goal: persistent storage and offline capability. Success: chunks are cached to disk (~/.cache/wormhole), reads hit RAM then disk then network; reboot and read again without re-downloading.
